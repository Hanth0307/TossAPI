"""Market data / news / disclosure ingestion interfaces.

Depends on `app.core` and `app.adapters` only. No concrete provider
(Toss quotes, DART filings, news feeds, ...) is implemented in Phase
00, and no response field names are assumed - see `base.py`.
"""

"""Trading strategy interfaces: turn market data + signals into orders.

Depends on `app.core`, `app.data`, and `app.models`. No strategy logic
or assumed return/performance figures are implemented in Phase 00.
"""
